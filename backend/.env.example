OPENROUTER_API_KEY=sk-or-v1-...
PORT=3001
HOST=0.0.0.0
NODE_ENV=development
# Optional: protect /api/logs endpoints. If unset, endpoints are open.
DASHBOARD_TOKEN=
# Optional: path to SQLite db file. Defaults to logs.db in project root.
DB_PATH=
